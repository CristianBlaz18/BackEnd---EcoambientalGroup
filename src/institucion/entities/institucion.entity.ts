import { Column, Entity, OneToMany, PrimaryGeneratedColumn } from "typeorm";

// @Entity({name: 'tbl_institucion'})
export class Institucion {
    // @PrimaryGeneratedColumn({name: 'institucion_id'})
    // id: number;
    // @Column({name: 'institucion_nombre', type: 'varchar', length: 100})
    // nombre: string;
    // @Column({name: 'institucion_descripcion', type: 'varchar', length: 500})
    // descripcion: string
    // @Column({name: 'institucion_logo', type: 'blob'})
    // logo: string;
    // @Column({name: 'institucion_slogan', type:'varchar', length: 100})
    // slogan: string;
    // @Column({name: 'institucion_mision', type: 'varchar', length: 500})
    // mision: string;
    // @Column({name: 'institucion_vision', type: 'varchar', length: 500})
    // vision: string;
    // @Column({name: 'institucion_vista_certificado', type: 'varchar', length: 500})
    // vista_certificado: string;
    // @Column({name: 'institucion_estado', type: "tinyint"})
    // Estado: number;
    
    //@OneToMany(()=> Publicidadslider, (publicidadslader)=> publicidadslader.institucion)
    //publicidadslider: Publicidadslider[];

    //@OneToMany(()=> Especializacion, (especializacion) => especializacion.id)
    //especializacion: Especializacion[];

}
